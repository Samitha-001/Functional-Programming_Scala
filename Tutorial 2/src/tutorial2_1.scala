object tutorial2_1 {
  def main(args: Array[String]): Unit ={
    var k, i, j = 2
    var m, n = 5
    var f = 12.0f
    var g = 4.0f
    var c = 'X'

    var res0 = k + 12 * m
    var res1 = m / j
    var res2 = n % j
    var res3 = m/j*j
    var res4 = f + 10*5 + g
    var res5 = (i+1)*n
    println(s"res0 - $res0\nres1 - $res1\nres2 - $res2\nres3 - $res3\nres4 - $res4\nres5 - $res5")
  }
}
